
const nombre = 'Deadpool';
const real = 'Wade Winston';

const normal = nombre + ' is ' + real ;
const template = `${ nombre } is ${ real }`;

console.log( template );

const html = `
<h1>Hola</h1>
<p>Mundo</p>
`;

console.log( html );

